/**
 * Test setup — runs once before any test file.
 *
 * jest-dom adds the matchers (toBeInTheDocument / toHaveTextContent /
 * etc.) that make assertions read like "the user sees X" instead of
 * "the DOM contains a node matching this selector".
 */
/// <reference types="@testing-library/jest-dom" />
import "@testing-library/jest-dom/vitest";
